[package]
name = "vero-audit-guard-core"
version = "0.1.0"
edition = "2021"
description = "Core Rust implementation for Vero Audit Guard security protocols"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
